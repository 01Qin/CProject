#include <stdio.h>

#define max_days 30


int main()
{
    int enter_days, i; 
    float daily_hs[max_days], total_hs = 0.0, avarage_hs;
    printf("The program calculates the total hours worked during\na specific period and the average length of a day.");
    printf("How many days:");
    scanf("%d", &enter_days);

    printf("Enter the working hours for day %d:", i + 1);
    scanf("%d", &daily_hs[i]);



 


    printf("Total hours worked: %.1f", total_hs);
    printf("Average length of day: %.1f", avarage_hs);
    printf("Hours entered: %.1f", daily_hs);

}


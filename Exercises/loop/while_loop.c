#include <stdio.h>

int main()
{
    int i = 0; // the value of i is initialised as 0

    // while(conditional expression) 
     // statement

    while (i <= 10)
    {
        printf("%d\n", i);
        i++;
    }
    return 0;
}
#include <stdio.h>

int main()
{
    char character_variable;
    character_variable = 'v';

    printf("THe character corresponding to the number %d is %c", character_variable,character_variable);
    return 0;
}